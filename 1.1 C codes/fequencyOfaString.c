#include <stdio.h>
#include <string.h>
#include <ctype.h>
int main()
{
    typedef char* p2s;
    typedef int* p2i;
    typedef float* p2f;
    char s[1000];
    string[]
    int l = strlen(s);
    char c;
    char freq[26] = {0};
    for(int i = 0 ; i<l ; i++)
    {
        if(s[i] == )
        {

        }
    }
}