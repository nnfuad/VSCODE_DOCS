#include <stdio.h>
int main()
{
    printf("Enter the elements to be stored in the first array:");
    int a,b;
    printf("Enter the elements to be stores in the second array:");

    int arr2[m];
    int arr2[n];
}