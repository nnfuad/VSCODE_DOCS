#include <stdio.h>
#include <string.h>

int main()
{
    char max[10000];
    char min[10000];
    char a[10000];
    int maxw;
    fgets(a,sizeof(a),stdin);
    for(int i = 0 ; i<=strlen(a) ;i++){
        max       
            
        
    }
}